#!/bin/sh

echo "Post-commit: Updating sync state..."

# Update session with commit info
COMMIT_MSG=$(git log -1 --pretty=%B)
COMMIT_HASH=$(git rev-parse --short HEAD)

../scripts/update-session.sh "Committed: $COMMIT_MSG ($COMMIT_HASH)" "claude-code"

echo "Sync state updated"
